import { PageableListComponent } from './pageable-list-component';

describe('PageableListComponent', () => {
  it('should create an instance', () => {
    expect(new PageableListComponent()).toBeTruthy();
  });
});
