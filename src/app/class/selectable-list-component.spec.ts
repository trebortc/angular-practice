import { SelectableListComponent } from './selectable-list-component';

describe('SelectableListComponent', () => {
  it('should create an instance', () => {
    expect(new SelectableListComponent()).toBeTruthy();
  });
});
